#include <bits/stdc++.h>
using namespace std;
vector<int> v;
void sub(string s, int i)
{
    if (i > -1)
    {
        if (i < s.size())
        {
            cout;
        }
    }
}

int main()
{

    return 0;
}